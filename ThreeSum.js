const ThreeSum = (array) => {
  let left = 0;
  let right = 0;
  for (let i = 0; i < array.length; i++) {
    let total = 0;
    array[left] + array[right];
  }
};
console.log(ThreeSum([1, 2, -3, -4, 2, 5]));
